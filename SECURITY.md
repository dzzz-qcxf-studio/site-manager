# 安全策略

## 支持范围

默认只支持仓库默认分支的最新代码。发布包和服务器安装脚本也属于安全边界的一部分。

## 报告安全问题

请优先使用 GitHub 的私密 Security Advisory 联系维护者，不要在公开 Issue、README、日志或截图中粘贴：

- SSH 私钥、密码、访问令牌或云厂商密钥；
- `settings.json`、SQLite 缓存或传输历史；
- 未脱敏的服务器日志、主机指纹和内部路径组合。

如果无法使用私密 Advisory，请先通过组织管理员提供的私下渠道联系维护者，并提供复现步骤、受影响版本和最小必要日志。

## 处理承诺

收到报告后会确认影响范围，必要时先撤回受影响的公开凭据，再修复代码、补充测试并发布安全说明。任何已经暴露的密钥都应立即在服务器或云平台侧轮换，不能只依赖删除 Git 提交。

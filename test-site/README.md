# 展台测试舱

这是一个无需构建的静态网页测试项目，用于上传到“网页展台”。

## 使用方法

1. 在网页展台里选择本文件夹 `test-site`。
2. 扫描通过后填写名称并发布。
3. 打开发布链接，点击“运行检查”。四项资源都显示绿色 `✓` 即表示静态资源可以正常读取。
4. 另外可以旋转 3D 立方体、打开 SVG/MP4/GLTF 原文件，并拖入本地文件测试浏览器交互。

入口文件必须是根目录的小写 `index.html`。项目不依赖 CDN 或构建工具。
